name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ published ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0  # Fetch all history and tags for version detection
      
      - name: Install uv
        uses: astral-sh/setup-uv@v9
        with:
          enable-cache: true
      
      - name: Set up Python
        run: uv python install 3.13
      
      - name: Install dependencies
        run: uv sync --dev
      
      - name: Lint with ruff
        run: uv run ruff check
      
      - name: Check formatting
        run: uv run ruff format --check
      
      - name: Type check
        run: uv run ty check
      
      - name: Run tests
        run: uv run pytest
      
      - name: Security audit
        run: |
          # Just warnings for now...
          uv audit --preview-features audit || true 

  build-and-publish:
    needs: lint-and-test
    runs-on: ubuntu-latest
    # Only version releases publish a package. Experiment/provenance releases
    # (e.g. `run-001`, which carries the Run 001 transcripts) must not get a
    # wheel attached -- same rule the version detection in pyproject.toml uses.
    if: >-
      github.event_name == 'release'
      && startsWith(github.event.release.tag_name, 'v')
    permissions:
      contents: write  # Required for uploading release assets
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0  # Fetch all history and tags for version detection
      
      - name: Install uv
        uses: astral-sh/setup-uv@v9
        with:
          enable-cache: true
      
      - name: Set up Python
        run: uv python install 3.13
      
      - name: Install dependencies
        run: uv sync --dev
      
      - name: Build package
        run: uv run python -m build
      
      - name: Upload artifacts to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          fail_on_unmatched_files: true
